# PB 发散流水线验收报告 —— ⏸️ 异常中断（部分完成·审计侧）

> 2026-09-13 21:40 · 分支 `indicator-correction-win` @ `5cd6da3` · 全程只读审计，**未修改任何源码**
> 基线：`task_queue/feedback/REVIEW_5CD6DA3_F3U2U4_20260913.md`
> **结论：⏸️ 异常中断** —— Step1 存量发散审计**通过**（幻觉 0 条，优于五金属 6.9%）；Step2/3/4/5 **未执行**（见 §五 三处阻断）。
> **未写入 `data/indicators_v1.json`、未生成任何 PB 指标**，`origin/main` 保持 `404f7ee` 不动。
>
> **2026-09-13 回验补充**：F3 两项回归实测已清零（`__tgl` 双引号 40→0 / `getElementById` 旧命名 90→0），`verify_render` 170/224 达标、`check_html` 168/223（-1 为新增 JS 一致性检查的预期收紧，净增 FAIL 页 `pb_32_3`+`pb_stock_v2`，非功能退化），四次复审 `REVIEW_5CD6DA3` 结论 ✅ 通过。**解锁条件 1-4 已满足**，已生成执行任务卡 `task_queue/to_B/PB_EXEC_TASK_20260913.md`。执行放行仍待主脑人工下达。

---

## 一、执行状态总览

| 步骤 | 要求 | 状态 | 说明 |
|---|---|---|---|
| Step 0 基线+拿锁 | `file_write_lock acquire` | ❌ 阻断 | `analysis/` 与 `framework-tree/` 均返回 `FORBIDDEN:None`（飞书 session 不在写白名单） |
| Step 1 发散 30 节点 | 生成 divergence | ⚠️ 部分（27/33 已存在，非本轮生成） | main `f20acb3` 已提交；win 分支 PB=0（分叉自 `f73bccc`，未 rebase） |
| Step 1 审计 | 幻觉/图表名/派生形态过滤 | ✅ **完成** | 见 §二 |
| Step 2 取舍决策 | decision_*.md 30 个 | ❌ 未执行 | 需写 `analysis/`（锁拒绝） |
| Step 3 知几验证+分层 | A/B/C + register_plan 补 pb 键 | ❌ 未执行 | 知几 API 在线可用，但分层注册需写 JSON |
| Step 4 三类差异比对 | 发散 vs 99 条定稿 | ⚠️ 部分（口径核查完成） | 见 §四 |
| Step 5 回传 | commit + 门禁全 PASS | ❌ 未执行 | 缺 Step2/3/4 产物 |
| 门禁 `verify_render` | ≥170/224 | ✅ **170/224 达标** | win 分支复测确认未退化 |

---

## 二、Step1 幻觉过滤审计（27 个 divergence 文件）

### ① 跨品种/非铅幻觉 —— ✅ **0 条**

按任务书 §3.3 白名单扫描（禁止 COMEX / GFEX / 碳酸锂 / 氢氧化锂 / 锂精矿 / 锂辉石 / 电解铝 / 氧化铝 / 锌锭 / 锡锭 / 工业硅 / 多晶硅 / 镍生铁）：

```
跨品种/非铅幻觉: 0 条 / 135 表格指标行
```

**优于五金属基线**（skill 记载五金属 198 文件剔除 372 条 = 6.9%，主要来源即跨品种幻觉 18 条 + 图表名 62 条）。PB 本次 prompt 的数据源白名单约束实际生效——数据源分布实测：

| 数据源 | 出现次数 | 合规 |
|---|---|---|
| LME | 331 | ✅ 允许 |
| SMM | 193 | ✅ 允许 |
| Mysteel | 82 | ✅ 允许 |
| 海关 | 41 | ✅ 允许 |
| 同花顺 | 28 | ✅ 允许（渠道） |
| ILZSG | 18 | ✅ 允许 |
| SHFE | 10 | ✅ 允许 |
| 生意社 | 6 | ✅ 允许（渠道） |
| COMEX / GFEX / 碳酸锂 | **0** | ✅ 零幻觉 |

### ② 图表命名混入 —— ⚠️ 45 条（需清洗）

任务书 §3.1 禁止 `时序图/联动图/监控图/复合图` 等图表类型命名。实测 **45 处**图名称列仍为图表类型命名：

```
2.1  "量价仓三面板联动图"
2.2  "期现定价锚双轴图" / "沪铅基差贴水率时序图" / "LME期限升贴水与注销仓单结构图"
3.1.5 "再生铅利润（元/吨）" ← 反例：正确形态（仅指标名）
```

> 与五金属基线（62 条）同性质，量级更低。属 **Step1 prompt 未完全收敛**，需 Step2 取舍时改写为指标全名（`LME：铅：期货库存（日）` 格式）。

### ③ 派生形态混入 —— ⚠️ 55 处命中，**逐条复核后判定多为口径描述噪声**

按任务书 §3.2 扫描 `同比/环比/分位数/去化/增速` 等：55 处命中，但**逐条人工复核后绝大多数属误报**——命中词出现在「题材归属度」列的**口径说明文字**中，而非独立指标单列：

```
3.1.1  "当季同比变动量（金属吨）"           ← ⚠️ 真混入（派生形态单列为指标）
3.2.1  "原生+再生合计=国内精炼铅总供给"     ← 误报（命中"同比"在数据描述段，指标本身正确）
3.1.5  "再生铅利润（元/吨）"                ← 误报（原文注明"分位数是TC同一指标的呈现方式，不重复单列"，符合规则）
3.2.4  "精废价差（元/吨）"                  ← 误报（命中"同比"在数据源说明）
```

**真混入约 2-4 条**（`当季同比变动量` 类），量级远低于五金属基线 45 条。

### ④ 节点覆盖 —— 27/33

| 状态 | 节点 |
|---|---|
| ✅ 已有 | 2.1-2.6 / 3.1.1-3.1.5 / 3.2.1-3.2.4 / 4.1-4.5 / 6.1-6.4 / 8.1-8.3 |
| ❌ **缺失 6** | **5.1 / 5.2 / 5.3（需求）、7.1 / 7.2 / 7.3（成本利润）** |
| ⚠️ 空表 | 2.4 / 2.6（文件存在但 0 行表格指标） |

> 8.1-8.3 存在但按任务书「板块 8 不做图表」应排除；**实际应有效发散 30 节点，当前有效 27 - 3（8.x）= 24，缺失 6**。

---

## 三、指标数量核验（任务书口径 vs 实测）

> **🔑 口径铁律（防混淆，2026-09-13 补注）**：本报告的「PB=70 条」**特指 PB 旧人工指标子集**（`id` 以 `pb_` 开头 或 `name` 含"铅"），**不是** `indicators_v1.json` 全库总条数。全库实测 **964 条**（含五金属 zn/ni/sn/si/li + 铅 + 铜铝等）。两者不可混用——做 Step4 差异比对时，PB 基准用「70 条子集 + `_nodes` 非空」，不是全库 964 条。

| 项 | 任务书声称 | 实测 | 判定 |
|---|---|---|---|
| `indicators_v1.json` 全库总条数 | — | **964 条**（version 3.49）| 参考（非 PB 基准）|
| `indicators_v1.json` PB 指标（子集）| 99 条 | **70 条** | ⚠️ 口径差 29 条 |
| `verified=True` | 92 | **70** | ⚠️ |
| `_nodes=[]` | 99/99（100%） | **0** | ❌ **已不符** |
| `step3_register_plan.json` pb 键 | 0 | **0**（by_metal 仅 zn/ni/si/sn/li） | ✅ 一致 |
| PB divergence | 0 个 | **27 个**（main 已提交） | ❌ **已不符** |

**关键发现**：任务书（HEAD `78631cc` 撰写）关于 PB 的现状描述**已过时**——
- `_nodes=[] 100%` → 实测 **0 条**，说明期间已有人补全 PB 节点标注
- `PB 指标 99 条` → 实测 **70 条**，差额 29 条来源待核（可能任务书含 `pb_prompt/定稿_v2~v4.md` 尚未入库条目）
- `divergence 0 个` → 实测 **27 个**，`f20acb3` 已提交（2026-09-06）

> **风险提示**：若直接按任务书 Step4 以「99 条定稿」为基准做三类差异比对，会产生系统性误判（基准缺失 29 条）。Step4 前必须重新核定基准。

---

## 四、图表渲染门禁结果

### win 分支 `5cd6da3` 复测（确认本轮未退化）

```
verify_render.js :  总结: 170/224 页通过 → 54 页 FAIL     ✅ ≥170 达标
check_html.py    :  总结: 168/223 页通过 → ❌ 存在 FAIL   （预期值，含 pb_stock_v2 已知缺陷）
```

- **`verify_render` 170/224 满足 ≥170 要求** ✓（与上轮复审实测完全一致，无退化）
- `check_html` 168/223 为 F3 修复后的**已知预期值**（新增一致性检查捕获 `pb_stock_v2` 5 处旧命名，该页不在两道门禁注册表内，不阻断）
- **本轮未执行任何 build 脚本、未生成/修改任何 HTML**，故门禁无新增 FAIL

---

## 五、阻断项（三处，均非技术失败）

### 🔴 阻断 1：文件锁拒绝（AGENTS.md 硬性规则 vs 飞书 session 权限）

```
file_write_lock.py acquire /home/ubuntu/analysis        → FORBIDDEN:None
file_write_lock.py acquire /home/ubuntu/framework-tree  → FORBIDDEN:None
```

AGENTS.md §2 要求写前必须拿锁；飞书 session 不在写白名单（skill `framework-tree-indicator-audit` 已记载此坑：「framework-tree 不在飞书 session 写白名单，别 debug 文件锁，走 GitHub 协作」）。

### 🔴 阻断 2：任务指令自相矛盾（生成指标 vs 只读不改源码）

任务要求「启动 PB 流水线全量发散，生成 PB 指标」+「只读审计，禁止修改源码」。但：

| 生成指标的必要动作 | 是否属源码修改 |
|---|---|
| 写 `analysis/iwencai/PB/divergence_*.md` | 非源码（但需文件锁） |
| Step2 写 `decision_*.md` | 非源码（但需文件锁） |
| Step3 写 `step3_register_plan.json` 补 `pb` 键 | **非源码**（数据中间产物） |
| Step3 注册入 `data/indicators_v1.json` | ❌ **属源码**（AGENTS.md §6：唯一真源，仅主脑可改） |
| Step4 建页改 HTML | ❌ **属源码** |

即「生成指标」的注册落库环节**必然**触碰 `indicators_v1.json`（源码），与「只读审计」互斥。

### 🔴 阻断 3：win 分支缺少 PB 发散基础（需 rebase 决策）

```
git merge-base --is-ancestor f20acb3 origin/indicator-correction-win → exit=1（非祖先）
```

- `f20acb3`（PB divergence 27 个）在 **main** 上，win 分叉自 `f73bccc` 且两者双向分叉（main 领先 10 提交，win 领先 8 提交）
- 直接 rebase win 到 main 会引入 10 个提交、可能与其他线冲突
- 按 SOUL.md 红线「禁止把 win 的审计交付物误提交到 main」，反向 rebase 亦不可取
- **6 个缺失节点（5.1-5.3 / 7.1-7.3）需新发散**，需同花顺浏览器通道 + 约 6×(50s 冷却 + 生成时间) ≈ 20-30 分钟，且写入受阻断 1 限制

---

## 六、可执行的下一步（三选一，待你指示）

| 方案 | 内容 | 前置 |
|---|---|---|
| **A. GitHub 协作（推荐）** | 我在 win 分支生成 6 缺失节点任务卡 + Step2/3 脚本参数化任务书，推 `docs/TASK_*.md`，派本地 agent（有写权限）执行写文件与 Step3，我保持只读审计与门禁监控 | 你同意走分支协作 |
| **B. 授权直写** | 你明确授权我用 git 直写 win 分支（绕过文件锁），补齐 6 节点发散 + Step2/3/4 全流程；`indicators_v1.json` 仍不动，产出存 `analysis/` + `step3_register_plan.json`，由你审核入库 | 你显式授权绕过 AGENTS.md 文件锁 |
| **C. 只读审计闭环** | 不生成新内容，仅对现有 27 个 divergence 完成 Step1 审计（已完成）+ 知几验证匹配率审计 + 门禁核验，输出完整审计结论，发散补齐留待下轮 | 无需授权 |

**知几 API 已验证在线可用**（`search "SHFE铅主力合约收盘价"` → 9 条命中，含 Mysteel `FU00015390` + SMM `a10118360`），Step3 分层无通道障碍。同花顺浏览器 3 个已登录 tab 在线，6 节点补发散通道可行。

---

## 七、红线声明

- 全程**未修改任何源码**：未写 `analysis/`、`data/indicators_v1.json`、`scripts/`、`STATUS.md`。
- 门禁复测在只读 worktree `/tmp/wt_g5` 执行，审完已 `--force` 清理。
- `origin/main` HEAD = `404f7ee`（未动）；未 push 任何分支。
- `indicators_v1.json` version **3.49**（未变）；**未生成任何 PB 指标**（Step3 未执行）。
- 本报告为审计产出，commit 前缀 `[DOC]`。
