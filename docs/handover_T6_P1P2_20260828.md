# T6 交接文档 — P1/P2 完成 + 季节按钮修复待上线验收（2026-08-28）

> 给**新会话**读。第一步：读本文件 + `framework-tree/STATUS.md`（最后一行 = P1/P2 变更记录）。
> GitHub 仓库：`algo23-yunqingtian/framework-tree`（SSH 推送）。当前分支：`feature/fix-seasonal-button`。

---

## 一、一句话状态

**P1（chart-kits 抽模块）+ P2（自动验证门禁）已全部完成，本地三道验证全绿。**
但**尚未 merge + push 上线**——线上 GitHub Pages 还是旧版（季节按钮假数据）。
用户实测线上 6.1/6.2/6.3「点季节看不见」= **线上旧版行为**，不是 P1/P2 的问题。

## 二、已完成的改动（本地 feature/fix-seasonal-button，commit 63ee6c4）

### P1：scripts/chart_kits.py 公共模块（269 行，新增）
- `load_metric / pairs / latest / sub_series`：读 api_cache.db 数据层
- `chart_line_t / chart_dual / chart_triple`：三套图表模板（含图备注 + 季节切换）
- `CSS / ANTI / JS_COMMON(cids) / page_html() / write_html()`：样式 + 反拷贝 + 公共 JS + 拼装
- **4 个 build 脚本 220+ 行 → 82-90 行（-60%）**，只写"读哪个指标 + 画哪张图 + 什么备注"
- 数据零损失：i40=103 / i17=103 / i37=103 / i19=2194 等点数与旧版完全一致

### P2：自动验证门禁（新增 2 个文件）
- `scripts/check_html.py`（185 行）：7 项静态校验 × 4 页（字节下限 / chart 容器数 / chart-note 数 / cid 已 __inst / 公共 JS / 季节真数据 / v1.9 特征）
- `scripts/verify_render.js`（jsdom + ECharts mock 真实渲染）：62 项检查，**能抓出 check_html 抓不到的语义 bug**（见下）

### 顺手修的 3 个隐性 bug（P1/P2 过程发现）
1. **6.2/6.3 季节按钮假数据**（`data:[null×12]` 硬编码）→ 统一真数据 `window.__seasonalize(__d)`（按月份历年均值）
2. **6.4 缺 button CSS 样式 + 无公共 JS 封装** → 补齐（与其它 3 页结构一致）
3. **`__tgl` 按钮文字语义错配**：源码 `nxt==='ts'?'⏱ 时序':'☀ 季节'` 与初始按钮文字语义对撞 → 点击后文字看似不变。统一为「**按钮=点击后的视图**」语义：
   - 当前时序(ts) → 按钮显示「☀ 季节」（点了切季节）
   - 当前季节(se) → 按钮显示「⏱ 时序」（点了切回时序）

## 三、三道验证结果（本地，全部通过）

| 验证 | 方法 | 结果 |
|---|---|---|
| check_html.py | 静态 7 项×4 页 | ✅ 4/4 |
| Node --check | JS 语法解析 | ✅ 4/4 |
| verify_render.js | jsdom + ECharts mock | ✅ 4/4（62 项检查）|

## 四、待办（新会话第一步）

### 1. 用户验收季节按钮（P0+P1 合并效果）
用户需打开**线上新版**点「☀ 季节」按钮确认能出真数据：
- 目前线上还是旧版 → **需先 merge + push 上线**
- merge 命令：
  ```
  cd /home/ubuntu/framework-tree
  git checkout main
  git merge feature/fix-seasonal-button
  git push origin main        # SSH 推送，触发 GitHub Pages 构建
  ```
- push 后 curl 验证线上（关键特征：`__seasonalize` 出现次数≥3）：
  ```
  curl -s --max-time 30 https://algo23-yunqingtian.github.io/framework-tree/pb_62_import_export.html | grep -c __seasonalize
  ```
- 用户确认 OK → 3 天观察期开始（无问题删备份）

### 2. 用户决定是否删除备份（3 天观察期）
- 备份位置：`/home/ubuntu/backups/framework-tree-t5-20260828/`
  - `framework-tree-repo.bundle`（998KB，git 完整历史）
  - `framework-tree-full-20260828.tar.gz`（3.1MB，全部文件）
  - `analysis-iwencai-20260828.tar.gz`（99KB，同花顺发散记录）
- git tag：`T5_P1P2_BEFORE_20260828`（P1/P2 前的完整快照点）
- **3 天内无问题 → 删备份 + 删 tag；出现问题 → `git checkout T5_P1P2_BEFORE_20260828` 回退**

### 3. 下次做锌/铜/锂 6.1-6.4 的用法（P1 核心收益）
```
cp scripts/build_pb_61.py scripts/build_zn_61.py
# 只改：指标ID + 图标题/sub/note + CIDS + 页面 title/hcrumbs/NOTE/footer
# 不需要再手抄 chart_line_t/chart_dual/chart_triple/CSS/ANTI/__seasonalize/__tgl
```

## 五、核心坑速查（本轮新增/验证）

| 坑 | 症状 | 解法 |
|---|---|---|
| **线上看的是旧版** | 本地改完线上没变，季节按钮还是假数据 | 先 curl 线上 grep `__seasonalize`（=0 说明没上线）；merge+push 后再验证 |
| **__tgl 按钮文字语义** | 点击后按钮文字看似不变 | 按钮=点击后的视图：ts→「☀季节」/ se→「⏱时序」；`__tgl` 与初始 `btn_txt` 必须同语义 |
| **check_html 抓不到语义 bug** | 静态字符串全对，实际交互错 | 必须上 verify_render.js（jsdom 真实调用 __tgl/__seasonalize）|
| **Node console.log 不支持 %s** | 输出 "%-42s" 字面量 | 用字符串拼接，别用 console.log('%s',x) |
| **jsdom 无 canvas** | echarts.init 报错 | beforeParse 注入 mock：`win.echarts={init:()=>({setOption(){},resize(){}}),getInstanceByDom:()=>null}` |
| **browser_navigate 禁本地地址** | 访问 127.0.0.1:8899 被 Blocked | 用 jsdom 渲染验证替代；或 browser_cdp |

## 六、参考文档

| 内容 | 路径 |
|---|---|
| 项目全局状态（唯一真源） | `framework-tree/STATUS.md` |
| 公共模块（P1） | `framework-tree/scripts/chart_kits.py` |
| 静态校验（P2） | `framework-tree/scripts/check_html.py` |
| 渲染验证（P2） | `framework-tree/scripts/verify_render.js` |
| build 脚本（4 份，瘦身后） | `framework-tree/scripts/build_pb_6X*.py` |
| 备份（3 天观察期） | `/home/ubuntu/backups/framework-tree-t5-20260828/` |
| 回退点 | git tag `T5_P1P2_BEFORE_20260828` |
| 流程 v2 定稿（历史） | `framework-tree/docs/handover_T5_flow_v2_20260828.md` |
