# HANDOVER 2026-09-04 — 同花顺批量分类范式 + framework-tree 主图核验

> 日期：2026-09-04（更新 15:30）
> 会话：飞书 feishu DM
> 目标：新对话直接开工，不看旧会话。只留「现在在哪 + 下一步 + 怎么动手」。

---

## 1. 同花顺分类范式批量任务（48/48 全部含答案主体 ✅）

**任务**：8 品种 × 6 板块 = 48 份「总→分」分类范式文档，同花顺问财批量驱动全自动产出。

**结果**：**48/48 全部含 AI 答案主体**，368 KB 产物落盘。

**核心 bug 修复**：`wait_for_completion` v2 — 旧版只看页脚就判定完成，导致答案还在流式输出时就截取（空/半成品）。新版 4 条件同时满足才判定：①页脚"内容由AI生成" ②"模型答案生成完成" ③答案关键词 ④长度稳定 15 秒。

| 品种 | 6 板块 | 平均单份大小 |
|---|---|---|
| 铜(CU) | 6/6 | 28 KB |
| 铝(AL) | 6/6 | 35 KB |
| 锌(ZN) | 6/6 | 42 KB |
| 铅(PB) | 6/6 | 42 KB |
| 镍(NI) | 6/6 | 58 KB |
| 锡(SN) | 6/6 | 63 KB |
| 工业硅(SI) | 6/6 | 70 KB |
| 碳酸锂(LC) | 6/6 | 76 KB |

**产物路径**：`/home/ubuntu/framework-tree/analysis/iwencai_classify/`（48 份 md，实测 48/48 在，2.6 MB）

**每份包含**：拆分树（总量→子类→孙类）+ 重点地区/工艺识别 + 指标推荐 + 优先级排序。

---

## 2. framework-tree 当前状态（实测，2026-09-04 08:20）

| 项 | 状态 |
|---|---|
| git HEAD | `19d0ec1`（v7 交接文档提交） |
| origin/main | `19d0ec1`（已 push，HEAD = origin/main） |
| 指标库 | **v3.62 / 1290 条** |
| 工作区 | 有未提交改动（`M ni_3_1_5.html` `ni_3_2_3.html` `ni_3_2_4.html` `ni_4_1.html` `ni_4_2.html` …）——**是 9/3 会话里修镍页主图的产物，未 commit 未 push** |
| 48 份 iwencai_classify md | 未纳入 git（在 analysis/ 下，可能未 track） |
| cron 汇报任务 | 挂着 `2c7837188437` 每日 09:00 `iwencai-classify-morning-report`（no_agent，deliver=origin 飞书）；**last_run_at=null（从未真跑过，脚本路径问题）** |

**已知未完成项**：
1. **工作区未提交**：镍页主图修复（`ni_*.html`）还在工作区，需 commit + push
2. **v3 审计器未落地**：`scripts/audit_mainchart_v2.py` 只扫了 91 节点（NI+SN+AL+CU），ZN/LI/SI/PB 共 117 节点未扫
3. **主图占位/错配修复**（v8 文档 §2）：31 页"SHFE主力收盘价"万能占位（NI 15 + SN 14 + AL 2）+ 9 页板块错配——**这批 ni_*.html 可能就是修了一部分，但没提交**
4. **明早 9 点 cron 汇报**：`iwencai_classify_report.py` 在 `~/.hermes/scripts/` 下，但 cron 引用路径需核对（当前 `script=iwencai_classify_report.py` 相对 `~/.hermes/scripts/`，实测脚本存在 2084 字节，理论应能跑）

---

## 3. 同花顺访问情况（关键，你问的重点）

**实测结论**：
- **批量脚本**：`/home/ubuntu/framework-tree/scripts/iwencai_classify_batch.py` ✅ 在（仓库内）
- **报告脚本**：`~/.hermes/scripts/iwencai_classify_report.py` ✅ 在（2084 字节，09:00 cron 引用）
- **python venv**：`/tmp/iwc_env/bin/python` ✅ 在
- **登录态 cookie**：❌ `/tmp/cookies.json` 和 `/tmp/iwencai_cookies.json` **都不存在**（实测 `ls` 返回 No such file）
- **知几 API**：`~/.hermes/scripts/zhiji_api.py` ✅ 在

**风险判定**：
- 昨晚 48/48 跑完时用的 cookie 已随会话销毁（/tmp 不持久）
- **下次再跑批量发散前，必须重新登录同花顺问财**（登录态用户"田允晴"）
- 同花顺问财需要浏览器登录，登录流程见 skill `iwencai-ai-answer-browser-ops`
- 明早 9 点 cron 汇报**不涉及同花顺访问**（只读本地 48 份 md），所以能正常跑

---

## 4. 下一步（按优先级）

### P0（紧急）
1. **commit + push 工作区的 ni_*.html**（9/3 修的镍页主图，未落盘）
2. **落地 v3 审计器**：把 ZN/LI/SI/PB 的 117 节点扫进来，拿到全 208 节点问题清单
3. **接入门禁**：v3 审计器红项=0 才允许 push

### P1（重要）
4. **修 31 个"SHFE主力收盘价"万能占位**（NI 15 + SN 14 + AL 2）+ 9 页板块错配
5. 按同花顺速查表逐节点核验：把 P0 指标写入 `MAIN_METRIC` 字典
6. 重跑 build + 门禁三连 + commit

### P2（后续）
7. SN 3.2.4（冶炼利润）+ SI 7.3（能源成本）发散建页（真缺口，0 指标）
8. 长期架构升级：页面读 data.json + layout.json（配置驱动）
9. 需求/成本利润板块的 P0-P4 范式（本次只问了库存/供给/进出口）

---

## 5. 关键文件路径

| 文件 | 路径 | 说明 |
|---|---|---|
| 指标库 | `framework-tree/data/indicators_v1.json` | v3.62 / 1290 条 |
| build 脚本 | `framework-tree/scripts/build_5m_batch.py` | 已 patch MAIN_METRIC + pick_main |
| v2 审计器 | `framework-tree/scripts/audit_mainchart_v2.py` | 只扫 4 品种，需升级 |
| 同花顺批量脚本 | `framework-tree/scripts/iwencai_classify_batch.py` | 48 份分类范式生成 |
| 同花顺报告脚本 | `~/.hermes/scripts/iwencai_classify_report.py` | 09:00 cron 引用 |
| 48 份分类范式产物 | `framework-tree/analysis/iwencai_classify/*.md` | 2.6 MB |
| 同花顺方法论 | `framework-tree/docs/iwencai_methodology_20260903.md` | 6 条标准 + 4 层校验 |
| 分板块 P0-P4 范式 | `framework-tree/docs/iwencai_board_paradigm_20260903.md` | 库存/供给/进出口 |
| 铜库存 P0-P4 演示 | `framework-tree/docs/iwencai_P0P4_copper_demo_20260903.md` | 完整分析示例 |
| 主图核验交接 | `framework-tree/docs/HANDOVER_mainchart_audit_20260903.md` | 9/3 版 |
| v8 主图专修交接 | `framework-tree/docs/HANDOVER_SUPERVISOR_v8_20260903.md` | 指标错配专修 |
| 知几 API | `~/.hermes/scripts/zhiji_api.py` | 返回 `results` 不是 `data` |
| 文件锁 | `~/.hermes/scripts/file_write_lock.py` | session 必须用飞书完整 ID |

---

## 6. 注意事项

1. **工作区 ni_*.html 未提交**：下次会话开工前先 `git status -s` + `git diff --stat` 确认，别覆盖
2. **同花顺 cookie 已失效**：下次再跑批量发散必须重新登录（浏览器，登录态用户"田允晴"）
3. **知几 API 今日配额可能受限**：9/3 实测 429 配额已尽（10000 次），今天恢复后再动
4. **cron 汇报 09:00 会跑**：`iwencai-classify-morning-report`（no_agent，只读本地 md，不涉及同花顺登录）
5. **MAIN_METRIC key 格式**：`"NI_4.1"`（品种_节点），不是 `"4.1"`
6. **pick_main 已改签名**：`pick_main(data, node, comm=None)`
7. **correction 文档有噪音**：同花顺 AI 回复本身含归属错配，不能盲抄
8. **data-harbor 数据断更 7 天**：LME 月价停在 2026-08-27，API 8765 活着（PID 1347273），是邮件源没喂，不是系统挂了

---

## 7. 新对话开工命令

```bash
cd /home/ubuntu/framework-tree
git fetch origin && git status -s          # 先看工作区脏不脏
git log --oneline -3                       # 确认 HEAD = origin/main
python3 -c "import json; d=json.load(open('data/indicators_v1.json')); print('指标:', len(d['indicators']))"  # 1290
# 若要跑同花顺批量：先重新登录 cookie，再
python3 scripts/iwencai_classify_batch.py --resume
# 门禁三连
python3 scripts/check_html.py && node scripts/verify_render.js && python3 scripts/reclaim.py
```
